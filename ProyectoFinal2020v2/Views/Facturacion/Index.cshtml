@model IEnumerable<ProyectoFinal2020v2.Models.Compra>

@{
    ViewData["Title"] = "Index";
}
@{ Layout = "~/Views/Shared/_AdminLayout.cshtml"; }
<h3 class="container p-3 my-3 text-white text-center rounded-l"
    style="background-color: #FFD700">
    <i class="fas fa-cart-arrow-down"></i> Compras
</h3>
@*<p>
        <a class="btn btn-success float-right " asp-action="Create">Create New <i class="fas fa-plus-square"></i></a><br />
    </p>*@

<table id="table" class="display" style="width:100%">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdCompra)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdEmpleado)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdCliente)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Fecha)
            </th>
            
            <th>
                <a class="btn btn-success" asp-action="Create">
                    Crear Nuevo
                    <i class="fas fa-plus-square"></i>
                </a>
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.IdCompra)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.IdEmpleado)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.IdCliente)
                </td>
                
                <td>
                    @Html.DisplayFor(modelItem => item.Fecha)
                </td>
                

                <td>
                    <div class="btn-group" style='font-size: 5px'>
                        
                        <a asp-action="Details" asp-route-id="@item.IdCompra" class="btn btn-secondary btn-sm">Detalles <i class="fas fa-align-justify "> </i></a> |
                        
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>

@section scripts{
    <script>
        $(document).ready(function () {
            $('#table').DataTable({
                "language": {
                    "url": "//cdn.datatables.net/plug-ins/1.10.15/i18n/Spanish.json"
                }
            });
        });
    </script>
}